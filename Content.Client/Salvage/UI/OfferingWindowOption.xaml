<PanelContainer xmlns="https://spacestation14.io"
                xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                HorizontalExpand="True"
                Name="BigPanel"
                Margin="5 0">
    <BoxContainer Orientation="Vertical"
                  Margin="5 5">
        <!-- Open Contract banner -->
        <controls:StripeBack Name="OpenContractStripe" Visible="False">
            <Label Name="OpenContractLabel"
                   HorizontalAlignment="Center"
                   Margin="0 5 0 5"/>
        </controls:StripeBack>
        <!-- Title box -->
        <controls:StripeBack>
            <Label Name="TitleStripe"
                   HorizontalAlignment="Center"
                   Margin="0 5 0 5"/>
        </controls:StripeBack>
        <BoxContainer Orientation="Vertical" Name="ContentBox"/>
        <!-- Reserve a compact confirmation area so the card height stays stable. -->
        <BoxContainer Name="ConfirmationBox"
                      Orientation="Vertical"
                      MinSize="0 48"
                      Margin="0 6 0 2">
            <RichTextLabel Name="ConfirmationWarningLabel"
                           Visible="False"/>
            <Label Name="ConfirmationClickLabel"
                   Visible="False"
                   HorizontalAlignment="Left"
                   Margin="0 3 0 0"/>
        </BoxContainer>
        <!-- Buffer so all claim buttons are in the same position -->
        <Control VerticalExpand="True"/>
        <Button Name="ClaimButton"
                HorizontalExpand="True"
                VerticalAlignment="Bottom"
                ToggleMode="True"
                Disabled="True"
                Text="{Loc 'offering-window-claim'}"/>
    </BoxContainer>
</PanelContainer>
