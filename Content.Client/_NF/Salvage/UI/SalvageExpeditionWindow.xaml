<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'salvage-expedition-window-title'}"
                      MinSize="760 320"
                      Resizable="True">
    <BoxContainer Orientation="Vertical"
                  VerticalExpand="True"
                  HorizontalExpand="True">
        <BoxContainer Orientation="Horizontal" Name="ProgressionBox" Visible="False">
            <Label Name="ProgressionLabel"
                   Text="{Loc 'salvage-expedition-window-progression'}"
                   SetWidth="96"
                   Margin="5"/>
            <ProgressBar Name="ProgressionBar"
                         HorizontalExpand="True"
                         MinValue="0"
                         MaxValue="1"
                         SetHeight="25"/>
            <Label Name="ProgressionText" Text="00:00"
                   Margin="5"/>
        </BoxContainer>
        <ScrollContainer HorizontalExpand="True"
                         VerticalExpand="True"
                         HScrollEnabled="True"
                         VScrollEnabled="True"
                         ReturnMeasure="True">
            <BoxContainer Orientation="Horizontal"
                          MinSize="900 0"
                          HorizontalExpand="True"
                          VerticalExpand="True">
                <!-- Private expeditions section with its own "next offer" timer -->
                <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                    <BoxContainer Orientation="Horizontal">
                        <Label Name="NextOfferLabel"
                               Text="{Loc 'salvage-expedition-window-next'}"
                               SetWidth="96"
                               Margin="5"/>
                        <ProgressBar Name="NextOfferBar"
                                     HorizontalExpand="True"
                                     MinValue="0"
                                     MaxValue="1"
                                     SetHeight="25"/>
                        <Label Name="NextOfferText" Text="0.00"
                               Margin="5"/>
                    </BoxContainer>
                    <controls:HLine Color="#404040" Thickness="2" Margin="0 5 0 5"/>
                    <BoxContainer Name="Container"
                                  Orientation="Horizontal"
                                  Margin="5 0 5 0"/>
                </BoxContainer>
                <!-- Spacer / visual divider between private and shared sections -->
                <Control SetWidth="10"/>
                <!-- Shared open contract section with its own timer -->
                <BoxContainer Orientation="Vertical"
                              HorizontalExpand="True"
                              MinSize="320 0">
                    <BoxContainer Orientation="Horizontal"
                                  HorizontalExpand="True"
                                  Margin="5 0 5 0">
                        <ProgressBar Name="SharedNextOfferBar"
                                     HorizontalExpand="True"
                                     MinValue="0"
                                     MaxValue="1"
                                     SetHeight="25"/>
                        <Label Name="SharedNextOfferText" Text="00:00"
                                                         Margin="5"/>
                    </BoxContainer>
                    <controls:HLine Color="#404040" Thickness="2" Margin="5 5 5 5"/>
                    <BoxContainer Name="SharedContainer"
                                  Orientation="Horizontal"
                                  HorizontalExpand="True"
                                  Margin="5 0 5 0"/>
                </BoxContainer>
            </BoxContainer>
        </ScrollContainer>
        <Button Name="Finish"
                Text="{Loc 'salvage-expedition-window-finish'}"
            HorizontalAlignment="Center"
            SetWidth="220"
            SetHeight="36"
            Margin="0 8 0 0"/>
    </BoxContainer>
</controls:FancyWindow>
